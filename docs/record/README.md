# The long-form project record

**This directory is the pre-2026-09-09 `CLAUDE.md`, moved here verbatim and
split at its top-level headings.** Concatenating `01-…` through `08-…` in
order reproduces that file byte for byte (checked with `diff` when the split
was made, at commit `6591360` on `feat/animation`).

**2026-09-14: that byte-for-byte property no longer holds, and has not since
2026-09-09.** Five commits edited `01`–`08` after the split (`18a137d`):
`916d607`, `2c1391d`, `2457da8`, `eff5776` and `cca5a18`
(`git log -- docs/record`). The SwiftUI-alignment record then added dated
errata lines to them. To recover the original file, run
`git show 6591360:CLAUDE.md` (4,172 lines); concatenating these files will not
reproduce it. `09-…` was never part of that file.

It was moved because the file had grown to 4,172 lines / 307 KB — roughly
75k tokens loaded into every session — and most of it is *record* (task-by-task
test-count climbs, quoted human reports, the history of how each sentence was
corrected) rather than *rules*. The rules are now in the root `CLAUDE.md`,
which points here for the reasoning.

**Every citation into the old file still resolves.** Test names, ruling ids,
divergence labels, greps and the wording of each paragraph are unchanged; only
the location moved. When a sentence here expires, correct it here and, if it
changes a rule, in the root `CLAUDE.md` too — the practices doc's
"correct where it was COPIED TO" mechanism applies across the two files.

| file | what it holds |
|---|---|
| `01-start-here.md` | the decisions-doc index and the per-subsystem essays (identity, `List`, `Deferred`, `Component`, `@State`, `@Observable`, hitboxes, focus, `Text`) |
| `02-practices.md` | the taxonomy's history: shapes 12–16 and record-mechanisms 1–7, with the incidents that produced each |
| `03-verified-on-real-hardware.md` | every milestone's human-verification entry: what was looked at, what was said, what a positive report closes, and the standing scripts |
| `04-divergences.md` | the divergences in full (eleven when split; `CLAUDE.md` lists twelve since divergence 19), plus the retired labels and why each was retired |
| `05-declared-but-inert.md` | the inert-API table with every row's full mechanism and grep |
| `06-build.md` | test/golden/guard counts milestone by milestone, the summary-line correction, the module-boundary staleness family |
| `07-layout-cost.md` | the per-node layout cost tables, the 100k cold frame, identity path construction |
| `08-when-ci-lands.md` | the guarantees that lapse silently (three when split, five items now; item 5, the freeze loop's allocation pin, appended 2026-09-14), and the guard count's moves: seven in the file, the eighth (35 → 39) dated in an erratum and in `09` — the first move whose guards mostly came after their hazard |
| `09-swiftui-alignment.md` | **not part of the split file.** The record of `a15ec83..7cfcddc` (2026-09-11 … 09-14): the proposal/measure/place kernel beside the CSS engine, the typed proposal modifiers and wrappers, stacks/priority/spacer, alignment, aspect ratio, the text bridge, `ProposalScrollView`, and the legacy `.padding` → `Box<Self>` change. It also covers each theme's pins, the SwiftUI probes (recorded as prose only, with no source in the repo), hazards, stale documents, what the plan leaves open, and the counts 993 / 97 / 39. **Appended 2026-09-14:** "Kernel completion (task 2)" (`3bb1ca1..553b980` on `feat/kernel-completion`): `ProposalLayout`, the boundary traps and invalidation contract, validation, depth guard and work counters, the two committed probes, the verifiers' mutation tables and the green mutations they found, counts 1084 / 97 / 45; dated corrections inline in the sections above |

| `10-modifier-composition.md` | plan task 3: `ModifiedElement`, the overlay-side id, the typed `ProposalNodeID`; lanes, red runs, verifier mutation tables. Lane 1's `M2`/`M3`/`M8`/`M11` and `R4` rows mutated the deleted `FrameModifier` and are historical |
| `11-environment.md` | plan task 9: scoped environment, the disabled gate, `KeyBinding`; four lanes, two verifier rounds, and the merge obligations |
| `12-accessibility-bridge.md` | plan task 12's bridge half: the `NSAccessibility` bridge, defaults and modifiers, `List` as a table; three lanes and their verifier rounds |
| `13-integration-tasks-3-9-12.md` | the integration of tasks 3, 9 and 12: merge resolutions, the red merge and the per-layer `AB-O` fix, the cross-track tests and their mutations, counts 1226 / 97 / 61, and the offscreen pixel stand-in for the untaken demo capture |
| `14-frame-and-sizing.md` | plan task 4 on `feat/frame-sizing`: the kernel's greedy flexible frame, the legacy frame's SwiftUI surface and its one lowering (`FrameSpec.style()`), the sizing inventory (`width`/`height` refused with measurements; `percent:` is a fraction); four lanes, the lost lane 1–2 verdicts reconstructed, lane 3–4 verifier tables, the pixel stand-in and `FR-U`/`FR-V`, counts 1247 / 97 / 63 |
| `15-outer-modifiers.md` | plan task 5 on `feat/outer-modifiers`: the wraps / self / paint-only / prepaint-only / distributes matrix and its table-driven test, `Component` padding wrapping per member, the legacy `border`/`focusBorder`/`opacity`/`clipped`/`allowsHitTesting`/`contentShape` modifiers with their two helpers and per-site guards, `borderWidth` deleted, divergence 15 fixed, five SwiftUI probes, four lanes' red runs and mutation tables, the verifier verdicts, the offscreen demo comparison, counts 1274 / 97 / 64 |
| `16-integration-tasks-4-5.md` | the integration of tasks 4 and 5: the `borderWidth` merge conflict, the interrupted session's un-reverted mutation, eight cross-track tests (frame layer × decorations, hit testing, accessibility, the disabled gate, the component side door, `contentShape` across a wrapper, the frame/background orders) and their mutations, probe arms S0–S3, divergence numbering 35–50, counts 1303 / 97 / 66, and the offscreen pixel stand-in with an instrument that sees the preview |
| `17-containers.md` | plan task 6 on `feat/containers`: SwiftUI's stack distribution, spacer, per-edge default spacing, typed alignments, `ZStack` and root placement, overlay/background content, the duplicate-parent trap and scroll axes on the proposal path; the one-node legacy frame and `fraction:`; the legacy audit and its pins; five lanes, their red runs, verifier verdicts (all five `ok`, lane 3 at the closeout) and mutation tables; the offscreen pixel stand-in; counts 1355 / 97 / 70; the Docs phase's re-take and probe revision 9; the branch checker; the closeout (the `hidden()` regression fixed, mutation F pinned by probe revision 10, 1357 / 97 / 70) at the end |
| `18-engine-replacement-stage-1.md` | plan task 7 stage 1 on `feat/engine-replacement`: the inventory of `FlexEngine` consumers, the fourteen-stage plan, the layout authority and per-site checks, the bounds log and differential harness, leaf/container/`Stack`/layer lowering, the demo content library, pipeline parity, depth and work pins; five lanes, red runs, verifier verdicts (all `ok`, lane 5 with four open minors) and mutation tables; the offscreen pixel stand-in and the chrome pair; counts 1409 / 97 / 71, re-taken after `swift package clean` at the Docs phase |
| `19-claude-md-full-2026-09-21.md` | the root `CLAUDE.md` verbatim as of `cb2e708` (120 KB), before it was cut to rules only to save context tokens. Every test name, ruling id, divergence row, inert-API row, human-verification row, performance figure and CI hazard the root file used to carry is here, section for section; the root file now points to it. **It is frozen at `cb2e708`**, so its divergence, inert and human-verification tables predate `21`–`23` below — for those read the dated sections at the end of `03`, `04` and `05` |
| `20-portable-scene.md` | `MetalUIScene`: `Scene` and the glyph atlas types (with the `FontKey` struct and `GlyphImage`) moved out of the Metal and CoreText modules into a target that imports only `MetalUIShaderTypes`; re-exports keep sources compiling; two plain-import guards for the `package` inits; the Linux build job and its CoreText mutant; counts 1411 / 97 / 73 |
| `21-engine-replacement-stage-2.md` | plan task 7 stage 2 on `feat/engine-stage-2`: flex-item semantics lowered by the parent — item records and the unconsumed report, the cross and main axes, the box model, distribution and reverse, animated fields; the two proposal-path answers that reach production; five lanes, red runs, verifier verdicts (all `ok`, fourteen minors) and mutation tables; the offscreen pixels, the chrome pair and the first real-window captures; counts 1460 / 97 / 71 |
| `22-grids.md` | plan task 7 stage G on `feat/grids`: SwiftUI's `Grid`/`GridRow` on the proposal path as a kernel `NativeNode` case — the plan and the nil solve, the finite solve's group/share/commit arithmetic, cell attributes and the modifier-chain walk, the elements and identity; four lanes, each red first, all verified (lane 2's two label-and-prose findings applied in the docs round); ten runnable probes and three recorded corpora, sixteen green mutations found (fourteen pinned, two equivalent), the offscreen and real-window comparisons reading the preview delta as exactly the grid's four cells; counts 1493 / 97 / 75 |
| `23-integration-stage-2-grids.md` | the integration of task 7's stages 2 and G on `integrate/stage-2-grids`: two conflict-free merges (both tracks appended to `LayoutTree.swift`), why `feat/grids`' stale `allOk: false` was merged, four cross-track tests at the one seam the two share — a grid cell whose content the lowering produced, a grid inside a lowered legacy container, one text measurement through both spellings, an item field on a cell reported unconsumed — and the five mutations proving them, including XM4, which found that the **lowered** half of lane 3's text clamp was pinned by neither track; the twelve-image comparison (nine at 0, the three preview images attributed cell by cell to the grids track's preview grid) and the locked-screen gate; and §6, the verification round that re-took the suite, the counts, four of the five mutations and every pixel figure from a rebuilt harness on three separately built archives — all identical — and fixed the two documents this integration's own claims pointed at; counts 1548 / 97 / 75. **Merged with `20`'s line on 2026-09-22**: the two together read 1550 / 97 / 77, and this side's record files were renumbered 19→21, 20→22, 21→23 because `19`/`20` above were already published |
| `24-freetype-rasterizer.md` | `MetalUIFreeType`: FreeType 2.14.3 vendored as `CFreeType`; `FreeTypeFont`/`FreeTypeRaster` matching `GlyphRaster`'s contract exactly, the outline-bbox fix that makes geometry agree in 832/832 oracle cases, and the dangling-pointer fix in font loading; two bundled open-licensed test fonts and the CoreText oracle suite with its measured tolerances and four pinned CoreText-departs-from-its-own-outline sizes; the separate `Tests/PortableTests` package pinning FreeType's output byte-for-byte on Linux and Windows CI; the `-Wno-shorten-64-to-32` fix for the default macOS build system; mutation tables for both suites; counts 1419 / 97 / 73 |
| `25-engine-replacement-stage-3.md` | plan task 7 stage 3 on `feat/engine-stage-3`: `ScrollView` lowered onto the kernel scroll viewport, one shared `ScrollChrome` for both scrollers, `Component` amend and wrap as per-member native frames, a frame layer over several members as a row, and both scroll suites running under both layout authorities (the exit test); five lanes, red runs, verifier verdicts (all `ok`, thirteen minors), mutation tables including the verifiers' twenty, the twelve offscreen images and the independently rebuilt harness; and §14, the branch checker's adversarial pass over `57893d0..HEAD` — every count re-measured from a clean tree, three of its own mutations (including the axis inversion, 20 of 34 scenarios on the `.proposal` arm alone), a fifth from-scratch `CN-R` harness reading 0 in all twelve, and three doc defects fixed; counts 1572 / 97 / 77. **Merged into `master` at `b10594c` on 2026-09-22** (§15): the two together read **1580 / 97 / 77**, this file was renumbered 24→25 because `24-freetype-rasterizer.md` above was already published, and §15.1 records the three-mutation interaction check — the two lines meet at `MetalUIScene.FontKey`, nowhere else |
| `26-harfbuzz-shaper.md` | `MetalUIHarfBuzz` on `feat/harfbuzz-shaper` (from `master` at the FreeType merge, record §24): HarfBuzz 14.5.0 vendored whole as `CHarfBuzz` with no pragma needed (unlike `CFreeType`); `HarfBuzzFont`/`HarfBuzzShaper` shaping in design units (`SH-D`) with no feature list (`SH-E`); a third bundled font (Noto Sans Arabic) and the CoreText oracle's 31-case corpus, its measured tolerances, the CoreText-has-no-per-glyph-offset representation trap, and the two pinned CoreText/HarfBuzz disagreements (mark clusters, digit direction) each with a measured, reverted-or-ruled-out alternative; `SH-H`'s FreeType rasterization tie-in; the portable package's second test target pinning 32 design-unit values byte-for-byte on Linux/Windows CI; eighteen mutations across both suites; counts 1573 / 97 / 77. **Merged into `master` at `f5e5651` on 2026-09-23**: renumbered 25→26 because `25-engine-replacement-stage-3.md` above was already published, and `master` then read 1595 / 97 / 77 (the "may be renumbered" note this row carried was stale from that merge on; branch checker, stage 4) |
| `27-engine-replacement-stage-4.md` | plan task 7 stage 4 on `feat/engine-stage-4`: `List`'s site check replaced by a windowed `ProposalLayout` placing realized rows at `(firstIndex + i) × rowHeight`, the rows as a group with the spacer demoted to a bare node, and `ListTests`, `TombstoneTests`, `FocusTests`, `AXNodeTests` and the two accessibility suites running under both layout authorities (67 scenarios, ten files, the roll call renamed and moved to `ZZ…`); the 100 000-row exit test at both authorities with hand-derived work literals; five lanes, three of whose red-befores are child-process aborts, verifier verdicts (all `ok`, fifteen minors), mutation tables including the verifiers' seventeen, the `CN-R` pixel harness **committed at last** and three real-window captures at 0; counts 1602 / 97 / 77. **Merged with `master` at `f5e5651` on 2026-09-23**: the two together read **1617 / 97 / 77**, and this file was renumbered 26→27 because `26-harfbuzz-shaper.md` above was already published |
| `28-portable-text.md` | `MetalUIPortableText` on `feat/portable-text` (from `master` at the HarfBuzz merge): the shared subpixel placement rule (`PT-C`), `PortableFont`'s cross-engine check and `PortableText.emit` (`PT-A`/`PT-B`/`PT-D`/`PT-E`); the Apple-path oracle's 384 cases, exactly equal in geometry (`PT-F`); `Experiments/SDLGPU`'s frame 4, the first text drawn with no Apple framework, 0 px against Metal on SDL Metal and Vulkan (`PT-G`); the portable package's third target pinning five emitted runs (`PT-H`); nine mutations, including the `xOffset` term only the Arabic pin sees and the `unitsPerEm` check no bundled font could reach until a patched 2048-unit face; counts 1603 / 97 / 77. Written as §27 and **renumbered 27→28 at the merge** with stage 4, which reached `master` first |
| `29-engine-replacement-stage-5.md` | plan task 7 stage 5 on `feat/engine-stage-5` (from `e5caefb`, before `master`'s portable-text merge): a `Deferred` whose one content node is `.position(.absolute)` becomes a presentation root under the proposal authority — element → greedy W on each stretched axis (aliased) → padding → a window-sized frame aligned per axis, laid out in its own native run before the root; every case where the legacy containing block is not the window, an absolute box outside a `Deferred`, and `minSize`/`maxSize` on an absolute box's auto axis each report by name instead of lowering to a different answer; divergences 9, 10 and 11 amended (9 survives, 10 unchanged and SwiftUI-backed, 11 legacy-only); three lanes, each with its own mutation table, all verified `ok`, two minor doc issues fixed in the Record phase; the exit test (`DeferredTests`, `AbsoluteOverlayTests`) plus the must-not-move set through real windows (`PresentationWindowTests`, six scenarios); roll call 67 → 82 across fifteen files; counts 1632 / 97 / 77 on this branch. **Merged with `master` at `42b9ab4` (the portable-text line) on 2026-09-23**: the two together read **1640 / 97 / 77**, and this file was renumbered 28→29 because `28-portable-text.md` above was already published (the precedent: record §23 §8, §25 and §27 headers) |
| `30-portable-line-breaking.md` | roadmap item 1 on `feat/portable-linebreak`: libunibreak 8.0 vendored as `CUnibreak` (`LB-A`); `PortableText.lineBreaks(in:)` in UTF-16 units and `PortableText.lines(_:font:wrappingAt:)` with `Shaper.shape`'s contract; the CoreText oracle over 13,464 cases at 0 boundary and 0 advance differences, and the five measured steps that got there (whitespace hangs, the paragraph's advances, cluster-then-grapheme emergency breaks, re-shaping inside a split ligature, 28 pt tab stops); cross-platform pins; eight mutations |
| `31-portable-lines-emit.md` | roadmap item 2 on `feat/portable-lines-emit`: `PortableFont.metrics` read from `hhea` (not `FT_Face`, which follows OS/2 under `USE_TYPO_METRICS` — found on patched faces), TrueType metrics rounded to a 16.16 fraction of the em and exact to the bit against CoreText, CFF within 6.4e-5 pt with one pinned whole-number case (`LB-F`); `PortableText.emitLines` (`LB-H`) equal in placement to `placedGlyphs` over 26,928 cases after three measured rules — CoreText's glyph substitutions, `units × (size / upem)`, and its 0xFFFF (`LB-I`); SDL frame 4's wrapped paragraph at 0 px (`LB-J`); pins (`LB-K`); seventeen mutations and two green ones explained |
| `32-portable-content-sizes.md` | roadmap item 3 on `feat/portable-content-sizes`: `PortableText.unbreakableRuns`, `minContentWidth`, `maxContentWidth` equal to the Apple path over 248 covered cases (`LB-N`); libunibreak asked for `"en-strict"`, found by a 4,418-pair class probe (347 → 123 → 0 differing, `LB-L`); Thai and a German-quote typesetter heuristic pinned wrong on purpose (`LB-M`); pins (`LB-O`); seven mutations, and the mutation runner's `failed after` blind spot, with item 2's table re-run |
| `33-portable-font-resolver.md` | roadmap item 4 on `feat/portable-font-resolver`: `PortableFontResolver` — registered faces matched by PostScript, family and full name ignoring case, `nil` and unmatched names to the default face, memoized per face and size (`FN-A`…`FN-C`), the same face as `CTFontCreateWithName` for 28 queries under each of three defaults; FreeType face names; pins (`FN-D`); nine mutations, one (R5) green until the oracle stopped sharing its default with a query target; system discovery split out as roadmap item 8b |
| `34-portable-core-layout.md` | roadmap item 5 on `feat/portable-core-layout`: `Package.swift` declares the Apple-bound targets under `#if os(macOS)` so the root package builds and tests on Linux and Windows unfiltered (`PC-A`); 486 layout and 22 core tests pass in `swift:6.4-noble`, goldens included, with WebKit and `malloc_logger` pieces compiled out per declaration (`PC-B`); typecheck guards read only this platform's build (`PC-C`); `scene-linux` builds and tests the root package, `root-windows` added |
| `35-text-seam.md` | roadmap item 6 on `feat/text-seam`: `MetalUITextSystem`'s `TextSystem` protocol with `CoreTextTextSystem` (today's path over the frame's `ShapingCache`) and `PortableTextSystem` (items 1–4), `Text`/`ProposalText`/`Frame.draw` on the seam, chosen once per app (`TS-A`…`TS-C`); the same tree through both systems draws identical sprites, and a portable frame never shapes through CoreText (`TS-D`); five mutations, one green until the oracle covered `ProposalText` |
| `36-render-seam.md` | roadmap item 7 on `feat/render-seam`: `WindowRenderer` in a now-portable `MetalUIPlatform` (`RS-A`), `MetalWindowRenderer` as the old inline Metal sequence (`RS-B`), AppKit moved to `MetalUIAppKit` (`RS-C`), `Experiments/SDLGPU/Portable` promoted to `Backends/SDL` with `SDLWindowRenderer` drawing byte-for-byte the replay path's frames and keeping its atlas (`RS-D`); five mutations |
| `37-sdl-platform.md` | roadmap item 8 on `feat/sdl-platform`: `SDLPlatform`/`SDLWindow` in `Backends/SDL` — events flattened in C and driven by synthetic SDL events in tests, points and pixel density, system theme, a run loop paced by the swapchain (`SP-A`); no accessibility, explicitly (`SP-B`); keys and wheels in AppKit's vocabulary so `Keymap` works unchanged (`SP-C`); six mutations, one a hang until the loop test got an iteration budget |
| `38-engine-replacement-stage-6a.md` | plan task 7 stage 6a on `feat/engine-stage-6a` (from `b3c29b9`, `master`'s tip after the stage-5/portable-text merge): the public `LayoutPass.requestNode`/`requestLeaf` deprecated, with every in-repo test caller (67 sites, 35 files) moved in the same change onto `requestNativeLeaf`/a `ProposalLayout` (authority-independent) or the internal, undeprecated `Frame.requestNode`/`requestLeaf` (a CSS answer or an unruled root-placement question); `UnlowerableField.owningStage` for `.customElement` moves to `"9"`; the entry measurement (the default authority and eight test-helper defaults flipped together, diagnostics on) classifies all 153 resulting reds as this stage's own baseline table for stage 6b (root placement, ~78 reds) and stage 7b (43 CSS reds); no divergence or declared-but-inert row changes, and no production behaviour moves beyond two `@available` attributes and one `owningStage` literal; three lanes plus a critic round, each with its own mutation table, one late reclassification (`FR-P`'s test 2.10, R → P-CSS-frame) found by lane 3's own verification; counts 1642 / 97 / 78 on this branch. **Merged with `master` at `64c5271` (PRs #11–#20, records §30–§37) on 2026-09-23**: the two together read **1688 / 97 / 78** (1686 + 2), and this file was renumbered 30→38 because `30-portable-line-breaking.md`…`37-sdl-platform.md` above were already published (the precedent: record §23 §8, §25, §27 and §29 headers) |
| `39-engine-replacement-stage-6b.md` | plan task 7 stage 6b on `feat/engine-stage-6b` (from `aef88ce`, `master`'s tip after the stage-6a merge): the root switch — `Frame.defaultLayoutAuthority` becomes `.proposal`, so `Frame.init`'s default and `Window.layoutAuthority`'s initial value both read it and **production now runs the proposal engine**; every red row of stage 6a's 92-row flipped-default table disposed by name (14 exit traps untouched — 12 after lane 1, `LR-DQ` item 1 — 2 rewritten, `hidden()` lowered closing 5 rows, a declared root axis's px/rem min/max folded, 73 of the 75 root-placement rows made independent of the default, 20 tests pinned `.legacy` with a named owner, 15 for 7b and 5 for 9); root placement ruled unchanged (`CN-J`, divergence 4 becomes legacy-authority only, retired at 7b); `NativeLayoutRun.maxDepth` re-bisected in release for the first time, 88 → 72; the demo's list row gains one declared height so its labels stay centred, its deepest native level 29 → 30; the fourteen-image offscreen comparison attributes every pixel delta to one of four named causes; exit test `noProductionFrameReachesTheLegacyEngine` green; three lanes, each with its own mutation table, all verified `ok`; the real-window capture and the demo-layout human-verification rows re-opened, both owed to the human (the screen was locked at every check); counts 1701 / 97 / 78 on this branch. Not yet merged with `master`. |

New milestones should append their record **here** (a new file or a new
section in the matching one) and put only the rule in `CLAUDE.md`.
