#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct type_Viewport
{
    float2 viewport;
    uint firstInstance;
    uint viewportPadding;
};

struct type_Projection
{
    float4x4 projection;
};

struct type_StructuredBuffer_v4float
{
    float4 _m0[1];
};

struct main0_out
{
    float2 out_var_TEXCOORD0 [[user(locn0)]];
    float2 out_var_TEXCOORD1 [[user(locn1)]];
    uint out_var_TEXCOORD2 [[user(locn2)]];
    float4 gl_Position [[position]];
};

vertex main0_out main0(constant type_Viewport& Viewport [[buffer(0)]], constant type_Projection& Projection [[buffer(1)]], const device type_StructuredBuffer_v4float& units [[buffer(2)]], const device type_StructuredBuffer_v4float& primitives [[buffer(3)]], uint gl_VertexIndex [[vertex_id]], uint gl_InstanceIndex [[instance_id]])
{
    main0_out out = {};
    uint _51 = gl_InstanceIndex + Viewport.firstInstance;
    uint _52 = _51 * 6u;
    float2 _61 = primitives._m0[_52].xy + (units._m0[gl_VertexIndex].xy * primitives._m0[_52].zw);
    uint _73 = _52 + 1u;
    out.gl_Position = Projection.projection * float4(((_61 / Viewport.viewport) * float2(2.0, -2.0)) + float2(-1.0, 1.0), 0.0, 1.0);
    out.out_var_TEXCOORD0 = _61;
    out.out_var_TEXCOORD1 = primitives._m0[_73].xy + (units._m0[gl_VertexIndex].xy * primitives._m0[_73].zw);
    out.out_var_TEXCOORD2 = _51;
    return out;
}

